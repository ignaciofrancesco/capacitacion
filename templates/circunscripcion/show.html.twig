{% extends 'base.html.twig' %}

{% block title %}Circunscripcion{% endblock %}

{% block body %}
    <h1>Circunscripcion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ circunscripcion.id }}</td>
            </tr>
            <tr>
                <th>Circunscripcion</th>
                <td>{{ circunscripcion.circunscripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_circunscripcion_index') }}">back to list</a>

    <a href="{{ path('app_circunscripcion_edit', {'id': circunscripcion.id}) }}">edit</a>

    {{ include('circunscripcion/_delete_form.html.twig') }}
{% endblock %}
