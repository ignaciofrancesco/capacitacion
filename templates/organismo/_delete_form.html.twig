<form method="post" action="{{ path('app_organismo_delete', {'id': organismo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ organismo.id) }}">
    <button class="btn">Delete</button>
</form>
